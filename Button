private var buttonView: some View {
    Button(action: self.tap) {
        Text(generator.answer.rawValue.localized())
            .multilineTextAlignment(.center)
            .lineLimit(2)
    }.animation(.easeInOut)
}
